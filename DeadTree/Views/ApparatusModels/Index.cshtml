@model IEnumerable<DeadTree.Models.DBClass.ApparatusModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Pattern)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pattern)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
